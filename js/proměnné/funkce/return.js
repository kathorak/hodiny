function prevod(km) {
    let mile = km * 0.6
    return mile //pro získání hodnoty např. pro pozdější práci
}

prevod(10)
console.log(prevod(10))

console.log(prevod(50))
let vysledekPrevodu = prevod(30)
